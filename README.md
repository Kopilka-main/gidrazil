# gidrazil

Минимальный статический сайт. Деплоится автоматически через GitHub Pages при push в `main`.

## Локально

Открой `index.html` в браузере — этого достаточно.

## Деплой

Любой push в `main` автоматически публикует сайт на GitHub Pages.
